import React from 'react'

const RegistrationForm = () => (

      <form>
        <input type="text"></input>
        <input type="password"></input>
        <button type="submit">Click me!</button>
      </form>
)

// JSX: The html / xml hybrid used in React that is the language of a react component that gets rendered.
// Uses tags and html elements to pass arguments to the tag /component
// class must be closed or self-enclosing if nothing between them
export default RegistrationForm
